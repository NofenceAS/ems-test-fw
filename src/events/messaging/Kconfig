# Copyright (c) 2021 Nofence AS
 
if LOG

menu "Event options"

comment "Initially logged events"

config LOG_MODULE_STATE_EVENT
	bool "Module state event"
	default y

config LOG_MSG_DATA_EVENT
	bool "MSG data event"
	default y

endmenu

endif