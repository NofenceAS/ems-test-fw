#include <event_manager.h>

/**
 * @brief Sends a request to the publisher module (simulated sensor) that
 * the simulated sensor should update its contents to the event bus
 */
void request_sensor_data();